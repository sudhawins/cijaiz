# React App API Configuration
# This URL is where the frontend sends API requests

# Local Development (server on localhost):
REACT_APP_API_URL=http://localhost:5000/api

# Docker/Container (service-to-service communication):
# REACT_APP_API_URL=http://server:5000/api

# Production:
# REACT_APP_API_URL=https://your-api-domain.com/api

# NOTE: In Docker environments, this value can be overridden by:
# 1. Environment variable in docker-compose.yml
# 2. Environment variable in deployed container
# 3. Value in this .env file (used during local development)
